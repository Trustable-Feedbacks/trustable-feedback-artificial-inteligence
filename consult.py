# Algorithm:
# Receive the request
# preprocess the input and lable
# Create the model
# If the forniced lable matches the AI result, return True, else, False.
# Return by logger the AI version, Result and Accuracy.